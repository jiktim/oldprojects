/* panic -- intentionally causes kernel panic / bsod
   Copyright (C) 2020 JIK, LLC.

   Written by: cth103 <cth103@free.fr>
*/

#include <stdio.h>

void kill() {
	// KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL
	// TODO: kill
}

int main() {
        kill();
        return 1;
}
